[package]
name = "antigravity-cli"
version = "0.1.0"
edition = "2021"

[dependencies]
antigravity-core = { path = "../core" }
clap = { version = "4.5", features = ["derive"] }
tokio = { version = "1", features = ["full"] }
